# 🏨 酒店搜索引擎前端 (HeyTrip & Hotel Search Engine Frontend)

[![Next.js](https://img.shields.io/badge/Next.js-14.0.3-black.svg)](https://nextjs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.0-blue.svg)](https://www.typescriptlang.org/)
[![React](https://img.shields.io/badge/React-18-blue.svg)](https://reactjs.org/)
[![Tailwind CSS](https://img.shields.io/badge/Tailwind%20CSS-3.3-38B2AC.svg)](https://tailwindcss.com/)
[![Shadcn/ui](https://img.shields.io/badge/Shadcn%2Fui-Latest-000000.svg)](https://ui.shadcn.com/)

> 基于 Next.js 14 + TypeScript 开发的现代化酒店搜索引擎管理后台，支持智能搜索、数据管理、权限控制等功能。

---

## 📋 目录

- [项目简介](#-项目简介)
- [核心特性](#-核心特性)
- [技术栈](#️-技术栈)
- [项目结构](#-项目结构)
- [快速开始](#-快速开始)
- [开发指南](#-开发指南)
- [功能模块](#-功能模块)
- [API 文档](#-api-文档)
- [部署配置](#-部署配置)

---

## 📖 项目简介

**酒店搜索引擎前端**是一个基于 Next.js 14 开发的现代化管理后台，为酒店搜索引擎系统提供完整的数据管理、用户管理、监控分析等功能。

### 核心功能

- **用户管理**: 完整的用户CRUD操作，支持角色权限管理
- **应用管理**: 应用配置、密钥管理、权限控制
- **系统监控**: 实时性能监控、健康检查、日志管理
- **缓存管理**: 智能缓存清理和统计
- **供应商管理**: 第三方供应商接入管理

### 技术特色

- **企业级架构**: 基于 App Router 的现代化 Next.js 架构
- **类型安全**: 100% TypeScript 覆盖，确保代码质量
- **组件化设计**: 基于 Shadcn/ui 的高质量组件库
- **响应式布局**: 支持多端适配的现代化UI设计
- **权限控制**: 基于JWT的完整认证授权体系

---

## ✨ 核心特性

### 🔐 认证系统

- ✅ **JWT Token 认证**: 完整的Token管理和刷新机制
- ✅ **角色权限控制**: admin、user、viewer 三级权限体系
- ✅ **路由守卫**: 自动跳转和权限验证
- ✅ **自动登录**: 支持记住我功能和自动登录

### 👥 用户管理模块

- ✅ **用户列表**: 数据表格展示，支持搜索、筛选、排序
- ✅ **创建用户**: 完整的表单验证和角色选择
- ✅ **编辑用户**: 数据预填充和实时更新
- ✅ **删除用户**: 安全确认和特殊警告（管理员账户）
- ✅ **批量操作**: 支持批量选择和处理
- ✅ **分页显示**: 高效的数据分页和加载

### 📊 数据管理

- ✅ **实时数据更新**: 操作后立即反映在界面上
- ✅ **搜索筛选**: 实时搜索和多维度筛选
- ✅ **排序功能**: 支持多字段排序
- ✅ **状态统计**: 实时统计数据展示

### 🎨 用户体验

- ✅ **响应式设计**: 完美适配桌面、平板、手机
- ✅ **加载状态**: 优雅的加载动画和进度提示
- ✅ **错误处理**: 完善的错误提示和用户反馈
- ✅ **确认对话框**: 重要操作的二次确认
- ✅ **无障碍访问**: 基于 Radix UI 的无障碍组件

---

## 🛠️ 技术栈

### 核心框架

- **Next.js 14.0.3** - React 全栈框架，支持 SSR/SSG
- **React 18** - 用户界面构建库
- **TypeScript 5** - 类型安全的 JavaScript 超集

### UI 组件与样式

- **Shadcn/ui** - 基于 Radix UI 的高质量组件库
- **Radix UI** - 无障碍访问的底层 UI 组件
- **Tailwind CSS 3.3** - 实用优先的 CSS 框架
- **Lucide React** - 现代化图标库

### 状态管理与数据处理

- **React Query (TanStack Query) 5.8** - 服务器状态管理
- **React Hook Form 7.47** - 高性能表单处理
- **Zod 3.22** - TypeScript 优先的模式验证

### 网络请求与加密

- **Axios 1.6** - HTTP 客户端
- **Crypto-JS 4.2** - 加密算法库（MD5 签名）

### 开发工具

- **ESLint + Prettier** - 代码规范与格式化
- **PostCSS + Autoprefixer** - CSS 处理

---

## 📁 项目结构

```
frontend
├── src
│   ├── app                       # Next.js App Router
│   │   ├── layout.tsx            # 根布局组件
│   │   ├── page.tsx              # 首页
│   │   ├── login                 # 登录页面
│   │   │   └── page.tsx
│   │   ├── dashboard             # 仪表盘页面
│   │   │   └── page.tsx
│   │   └── users                 # 用户管理页面
│   │       └── page.tsx
│   │   ├── favicon.ico           # 网站图标
│   │   └── globals.css           # 全局样式
│
│   ├── components                # 可复用组件
│   │   ├── auth                  # 认证相关组件
│   │   │   └── ProtectedRoute.tsx
│   │   ├── users                 # 用户管理组件
│   │   │   ├── CreateUserDialog.tsx
│   │   │   ├── EditUserDialog.tsx
│   │   │   └── DeleteUserDialog.tsx
│   │   ├── layout                # 布局组件
│   │   │   └── Header.tsx
│   │   └── ui                    # UI 组件库
│   │       ├── button.tsx
│   │       ├── card.tsx
│   │       ├── input.tsx
│   │       ├── form.tsx
│   │       ├── dialog.tsx
│   │       ├── table.tsx
│   │       ├── badge.tsx
│   │       ├── alert-dialog.tsx
│   │       ├── dropdown-menu.tsx
│   │       ├── label.tsx
│   │       └── select.tsx
│
│   ├── hooks                     # 自定义 Hooks
│   │   ├── auth                  # 认证相关 Hooks
│   │   │   └── useAuth.tsx
│   │   ├── api                   # API 相关 Hooks
│   │   └── localStorage          # 本地存储 Hooks
│
│   ├── lib                       # 工具库和配置
│   │   ├── auth                  # 认证逻辑
│   │   │   └── token.ts          # Token 管理
│   │   ├── api                   # API 客户端
│   │   │   └── client.ts
│   │   ├── constants             # 常量定义
│   │   │   └── index.ts
│   │   └── utils                 # 工具函数
│   │       └── utils.ts
│
│   ├── types                     # TypeScript 类型定义
│   │   ├── auth                  # 认证类型
│   │   ├── api                   # API 类型
│   │   └── common                # 公共类型
│
│   └── styles                    # 样式文件
│       └── globals.css           # 全局样式
│
├── docs                          # 项目文档
│   ├── 前端开发计划.md
│   ├── 前端开发任务清单.md
│   └── 前端问题记录.md
│
├── public                        # 静态资源
│   ├── file.svg                  # 文件图标
│   ├── globe.svg                 # 地球图标
│   ├── next.svg                  # Next.js 图标
│   ├── vercel.svg                # Vercel 图标
│   └── window.svg                # 窗口图标
│
├── .env.example                  # 环境变量示例
├── .eslintrc.mjs                 # ESLint 配置
├── .prettierrc                   # Prettier 配置
├── components.json               # Shadcn/ui 配置
├── next.config.ts                # Next.js 配置
├── package.json                  # 依赖配置
├── tsconfig.json                 # TypeScript 配置
└── README.md                     # 项目说明文档" README.md
```

### 核心模块说明

| 模块          | 职责                 | 文件                     |
| ------------- | -------------------- | ------------------------ |
| **认证模块**  | JWT认证、权限控制    | `src/lib/auth/token.ts`  |
| **用户管理**  | CRUD操作、对话框组件 | `src/components/users/`  |
| **布局组件**  | 头部、侧边栏、页脚   | `src/components/layout/` |
| **UI组件库**  | 基础组件封装         | `src/components/ui/`     |
| **API客户端** | HTTP请求封装         | `src/lib/api/client.ts`  |

---

## 🚀 快速开始

### 环境要求

- **Node.js 18+**
- **npm 9+** 或 **pnpm 8+**
- **Git**

### 安装依赖

```bash
# 安装依赖
npm install
# 或使用 pnpm
pnpm install
```

### 环境配置

```bash
# 复制环境变量模板
cp .env.example .env.local

# 编辑环境变量
nano .env.local
```

主要环境变量：

```env
# API 配置
NEXT_PUBLIC_API_URL=http://localhost:8080
API_BASE_URL=http://localhost:8080

# 认证配置
JWT_SECRET=your_jwt_secret_key
TOKEN_EXPIRE_TIME=86400

# 应用配置
APP_NAME=Hotel Search Engine
APP_VERSION=1.0.0
```

### 开发服务器

```bash
# 启动开发服务器
npm run dev
# 或使用 pnpm
pnpm dev

# 访问地址
# http://localhost:3000
```

### 构建生产版本

```bash
# 构建生产版本
npm run build
npm run start

# 或使用 pnpm
pnpm build
pnpm start
```

---

## 🛠️ 开发指南

### 代码规范

项目使用 ESLint + Prettier 确保代码质量：

```bash
# 代码格式检查
npm run lint

# 代码格式化
npm run format

# 类型检查
npm run type-check
```

### 项目约定

#### 1. 文件命名

- **页面文件**: 使用 `page.tsx` 命名
- **组件文件**: 使用 `PascalCase` 命名，如 `UserCard.tsx`
- **工具文件**: 使用 `camelCase` 命名，如 `apiClient.ts`

#### 2. 组件规范

- 使用函数式组件和 React Hooks
- 导出方式：`export default` 或命名导出
- Props 类型定义使用 TypeScript interface

#### 3. 样式规范

- 使用 Tailwind CSS 优先
- 自定义样式使用 CSS Modules
- 响应式设计使用 Tailwind 的断点系统

### 核心组件使用

#### 1. 认证组件

```typescript
import { ProtectedRoute } from '@/components/auth/ProtectedRoute';
import { AuthProvider } from '@/components/auth/AuthProvider';

// 路由保护
<ProtectedRoute requiredRole="admin">
  <AdminPanel />
</ProtectedRoute>

// 认证提供器（在 layout.tsx 中使用）
<AuthProvider>
  <App />
</AuthProvider>
```

#### 2. 用户对话框

```typescript
import { CreateUserDialog } from '@/components/users/CreateUserDialog';
import { EditUserDialog } from '@/components/users/EditUserDialog';
import { DeleteUserDialog } from '@/components/users/DeleteUserDialog';

// 创建用户
<CreateUserDialog onCreateUser={handleCreateUser} />

// 编辑用户
<EditUserDialog user={user} onEditUser={handleEditUser} />

// 删除用户
<DeleteUserDialog user={user} onDeleteUser={handleDeleteUser} />
```

#### 3. API 调用

```typescript
import { apiClient } from '@/lib/api/client';

// GET 请求
const { data } = await apiClient.get('/users');

// POST 请求
const result = await apiClient.post('/users', userData);

// 带认证头
const authData = await apiClient.get('/profile', {
  headers: { Authorization: `Bearer ${token}` },
});
```

---

## 🔧 功能模块

### 1. 认证系统 (`/login`)

- **功能**: 用户登录、退出、自动登录
- **技术**: JWT Token、React Hook Form、Zod验证
- **演示账户**:
  - 管理员: `admin/admin123`
  - 普通用户: `user/user123`
  - 查看者: `viewer/viewer123`

### 2. 仪表盘 (`/dashboard`)

- **功能**: 系统概览、快捷操作、统计图表
- **组件**: 统计卡片、快捷导航、系统状态
- **数据**: 用户统计、系统状态、性能指标

### 3. 用户管理 (`/users`)

- **功能**: 用户CRUD、角色管理、批量操作
- **特性**:
  - 实时搜索和筛选
  - 角色权限管理
  - 安全删除确认
  - 数据分页显示

#### 用户角色说明

| 角色       | 权限     | 说明                             |
| ---------- | -------- | -------------------------------- |
| **admin**  | 全部权限 | 系统管理员，拥有所有功能权限     |
| **user**   | 基本权限 | 普通用户，可以管理数据和查看信息 |
| **viewer** | 只读权限 | 查看者，只能查看数据，无法修改   |

### 4. 应用管理 (`/apps`)

- **功能**: 应用配置、AppId管理、密钥管理
- **特性**:
  - 应用注册和管理
  - 密钥生成和轮换
  - 权限配置
  - 限流策略设置

### 5. 系统监控 (`/monitor`)

- **功能**: 系统健康检查、性能监控
- **指标**:
  - 服务状态
  - 性能指标
  - 资源使用
  - 错误统计

### 6. 日志管理 (`/logs`)

- **功能**: 系统日志查看、搜索分析
- **类型**:
  - API 调用日志
  - 搜索日志
  - 同步日志
  - 错误日志

### 7. 缓存管理 (`/cache`)

- **功能**: 缓存清理、统计查看
- **操作**:
  - 全量缓存清理
  - 按名称清理缓存
  - 缓存命中率统计

### 8. 其他管理模块

- **供应商管理** (`/suppliers`): 第三方供应商接入管理
- **同义词管理** (`/synonyms`): Elasticsearch同义词配置
- **索引管理** (`/index`): 索引创建、删除、重建
- **定时任务** (`/scheduler`): 任务调度管理
- **Camel路由** (`/camel`): Apache Camel路由监控

---

## 📚 API 文档

### 认证相关 API

```typescript
// 登录
POST /api/auth/login
{
  "username": "admin",
  "password": "admin123"
}

// 获取用户信息
GET /api/auth/profile
Authorization: Bearer <token>

// 刷新Token
POST /api/auth/refresh
{
  "refreshToken": "refresh_token_here"
}
```

### 用户管理 API

```typescript
// 获取用户列表
GET /api/users?page=1&size=20&search=keyword&role=admin

// 创建用户
POST /api/users
{
  "username": "newuser",
  "email": "user@example.com",
  "password": "password123",
  "role": "user"
}

// 更新用户
PUT /api/users/{id}
{
  "username": "updated_user",
  "email": "updated@example.com",
  "role": "admin"
}

// 删除用户
DELETE /api/users/{id}

// 批量操作
POST /api/users/batch
{
  "action": "delete",
  "userIds": ["1", "2", "3"]
}
```

### 系统监控 API

```typescript
// 系统状态
GET / api / monitor / health;

// 性能指标
GET / api / monitor / metrics;

// 缓存统计
GET / api / monitor / cache / stats;
```

---

## 🚀 部署配置

### 开发环境

```bash
# 启动开发服务器
npm run dev

# 访问地址
# http://localhost:3000
```

### 生产环境

#### 1. 构建静态文件

```bash
# 构建生产版本
npm run build

# 预览构建结果
npm run start
```

#### 2. Docker 部署

```dockerfile
# Dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]
```

```bash
# 构建镜像
docker build -t hotel-search-frontend .

# 运行容器
docker run -d -p 3000:3000 --name hotel-frontend hotel-search-frontend
```

#### 3. Vercel 部署

```bash
# 安装 Vercel CLI
npm i -g vercel

# 部署到 Vercel
vercel

# 生产部署
vercel --prod
```

### 环境变量配置

#### 生产环境

```env
# 生产环境配置
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
API_BASE_URL=https://api.yourdomain.com

# 认证配置
JWT_SECRET=production_jwt_secret
NEXT_PUBLIC_APP_NAME=Hotel Search Engine
NEXT_PUBLIC_APP_VERSION=1.0.0
```

#### 环境变量说明

| 变量名                    | 描述        | 必需 | 默认值              |
| ------------------------- | ----------- | ---- | ------------------- |
| `NEXT_PUBLIC_API_URL`     | API基础URL  | ✅   | -                   |
| `API_BASE_URL`            | 后端API地址 | ✅   | -                   |
| `JWT_SECRET`              | JWT签名密钥 | ✅   | -                   |
| `NEXT_PUBLIC_APP_NAME`    | 应用名称    | ❌   | Hotel Search Engine |
| `NEXT_PUBLIC_APP_VERSION` | 应用版本    | ❌   | 1.0.0               |

---

## 🧪 测试

### 单元测试

```bash
# 运行所有测试
npm test

# 运行测试并生成覆盖率报告
npm run test:coverage

# 监视模式运行测试
npm run test:watch
```

### E2E 测试

```bash
# 运行端到端测试
npm run test:e2e

# 在CI环境中运行
npm run test:e2e:ci
```

### 测试工具

- **Jest**: 单元测试框架
- **React Testing Library**: 组件测试
- **Playwright**: E2E测试
- **MSW**: API Mock

---

## 📈 性能优化

### 1. 页面性能

- **代码分割**: 使用 `dynamic()` 实现路由级别的代码分割
- **图片优化**: 使用 Next.js Image 组件自动优化
- **预加载**: 使用 `next/prefetch` 预加载关键资源

### 2. 渲染性能

- **SSR/SSG**: 根据页面特性选择合适的渲染模式
- **缓存策略**: 使用 React Query 进行数据缓存
- **懒加载**: 组件和图片的懒加载

### 3. Bundle 优化

- **Tree Shaking**: 移除未使用的代码
- **动态导入**: 按需加载大体积依赖
- **压缩**: 生产环境自动压缩

---

## 🔍 故障排除

### 常见问题

#### 1. 路由访问404

**问题**: 访问 `/users` 页面返回404

**解决方案**:

```bash
# 检查文件路径是否正确
ls -la app/users/page.tsx

# 重新启动开发服务器
npm run dev
```

#### 2. TypeScript 编译错误

**问题**: TypeScript 类型检查失败

**解决方案**:

```bash
# 运行类型检查
npm run type-check

# 检查 tsconfig.json 配置
cat tsconfig.json
```

#### 3. 样式不生效

**问题**: Tailwind CSS 样式没有应用

**解决方案**:

```bash
# 检查 Tailwind 配置
cat tailwind.config.js

# 清理缓存重新构建
rm -rf .next
npm run dev
```

#### 4. API 请求失败

**问题**: 后端API调用返回错误

**解决方案**:

```bash
# 检查环境变量配置
cat .env.local

# 验证API地址是否可访问
curl -I http://localhost:8080/health
```

### 调试工具

- **React Developer Tools**: React 组件调试
- **Next.js DevTools**: Next.js 性能分析
- **Redux DevTools**: 状态管理调试
- **Network Tab**: 网络请求监控

---

## 📝 开发规范

### Git 提交规范

```bash
# 功能开发
git commit -m "feat: 添加用户管理功能"

# Bug修复
git commit -m "fix: 修复登录页面验证问题"

# 文档更新
git commit -m "docs: 更新API文档"

# 样式调整
git commit -m "style: 优化用户列表页面样式"

# 重构代码
git commit -m "refactor: 重构认证组件结构"

# 性能优化
git commit -m "perf: 优化用户列表渲染性能"
```

### 代码审查清单

- [ ] TypeScript 类型定义完整
- [ ] 组件 props 有默认值
- [ ] 错误处理完善
- [ ] 加载状态处理
- [ ] 响应式设计适配
- [ ] 无障碍访问支持
- [ ] 代码注释清晰
- [ ] 测试覆盖充分

---

## 🤝 贡献指南

### 开发流程

1. **Fork 项目** - 复制项目到自己的GitHub账户
2. **创建分支** - 从 `main` 分支创建特性分支
3. **开发功能** - 按照开发规范编写代码
4. **测试验证** - 运行测试确保功能正常
5. **提交代码** - 使用规范的提交信息
6. **创建PR** - 提交Pull Request等待审查

### 代码规范

- 遵循项目的 TypeScript 配置
- 使用 Prettier 格式化代码
- 确保 ESLint 检查通过
- 编写有意义的提交信息
- 添加必要的注释和文档

---

## 📄 许可证

本项目基于 MIT 许可证开源，详见 [LICENSE](LICENSE) 文件。

---

## 👨‍💻 团队

- **前端开发团队** - 负责前端架构设计和开发
- **后端开发团队** - 提供API接口和数据支持
- **产品设计团队** - 提供UI/UX设计方案

---

## 📞 支持与反馈

- **问题反馈**: [GitHub Issues](https://github.com/your-org/hotel-search-engine/issues)
- **功能建议**: [GitHub Discussions](https://github.com/your-org/hotel-search-engine/discussions)
- **技术文档**: [项目Wiki](https://github.com/your-org/hotel-search-engine/wiki)

---

**最后更新**: 2025年11月17日  
**版本**: v1.0.0  
**维护者**: 前端开发团队
